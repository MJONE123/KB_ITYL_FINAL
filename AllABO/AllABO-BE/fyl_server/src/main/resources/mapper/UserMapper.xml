<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.allabo.fyl.fyl_server.mapper.UserMapper">

    <select id="selectUserProfile" resultType="com.allabo.fyl.fyl_server.dto.UserDTO">
        SELECT id, name, identity_number, phone_number
        FROM customer
        WHERE id = #{userId}
    </select>


    <update id="updateUserProfile">
        UPDATE customer
        SET name=#{name}, identity_number=#{identityNumber}, phone_number=#{phoneNumber}
        WHERE id=#{id}
    </update>
    <update id="updateUserPassword" parameterType="UserResetDTO">
        UPDATE customer
        SET pwd = #{pwd}
        WHERE id = #{id}
    </update>

</mapper>
